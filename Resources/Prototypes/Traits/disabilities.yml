# HONK - Every former trait in this file was migrated to per-trait files under
# Resources/Prototypes/@RussStation/Traits/<category>/<trait>.yml so the fork can
# own costs, tags, and category assignments without fighting upstream on every
# rebase. Prototype ids are unchanged so existing player save data still resolves.
#
# This file is intentionally empty. When upstream adds a new disability trait the
# addition lands here as either a clean diff or a conflict against this comment,
# either way it will be visible during rebase and can be migrated to a fork file.
